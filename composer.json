{
    "name": "orsif/instagram-without-api",
	"type": "composer-plugin",
    "license": "MIT",
    "description": "A simple PHP code to get unlimited instagram public pictures by every user without api, without credentials (just token from cookies), just Instagram Scraping (with cookie and image data in base64).",
    "authors": [
        {
            "name": "Francesco Orsi",
            "email": "hello@orsi.me"
        }
    ],
    "minimum-stability": "dev",
    "require": {
        "php": ">=5.3.0"
    },
    "autoload": {
        "psr-0": {
            "InstagramWithoutApi": "src/"
        }
    }
}
